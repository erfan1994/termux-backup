# termux-backup
Termux Vault - Production-grade backup &amp; restore for Termux. Full system snapshots with zstd compression, SHA256 integrity verification, atomic operations. Self-contained, zero dependencies. Backup HOME, PREFIX, packages &amp; configs in seconds. Restore everything in minutes. Your Termux safety net.
